modules:
  http_2xx:
    prober: http
    timeout: 10s
    http:
      method: GET
      valid_http_versions: ["HTTP/1.1", "HTTP/2"]
      valid_status_codes: []  # 2xx is default
      follow_redirects: true
      preferred_ip_protocol: "ip4"
